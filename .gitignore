node_modules

# Keep environment variables out of version control
.env
.env.*
!.env.example

/generated/prisma

# Supabase local config (hanya berisi setting lokal, tidak perlu di-push)
supabase/.branches
supabase/.temp
